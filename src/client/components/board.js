import React from 'react';

import { List } from 'immutable';

const Board = (props) => {
		const list = props.list;

		return (
				<div>
						<p> Board </p>
				</div>
		);
}

export default Board;
